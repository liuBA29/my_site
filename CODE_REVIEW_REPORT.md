# Отчет о проверке кода сайта

**Дата:** 17 февраля 2026  
**Проект:** Django сайт (my_site)

## 🔴 Критические проблемы

### 1. Ошибка в middleware.py - использование несуществующих полей модели

**Файл:** `my_site/main_app/middleware.py` (строки 57-58)

**Проблема:**
```python
view.last_viewed_at = now()  # ❌ Поле не существует в модели
view.last_viewed_ip = ip     # ❌ Поле не существует в модели
```

**В модели PageView есть только:**
- `path`
- `views_count`
- `last_viewed` (не `last_viewed_at`)

**Последствия:** При каждом запросе будет возникать ошибка `AttributeError`, что может привести к падению сайта.

**Решение:** Исправить на:
```python
view.last_viewed = now()  # Использовать существующее поле
# Убрать строку с last_viewed_ip или добавить поле в модель
```

---

## ⚠️ Важные проблемы

### 2. Дублирование кода получения IP адреса

**Проблема:** Функция `get_client_ip()` дублируется в 3 местах:
- `my_site/main_app/views.py` (строки 118-125)
- `my_site/main_app/middleware.py` (строки 69-73)
- `my_site/accounts/signals.py` (вероятно)

**Решение:** Создать общую утилиту `my_site/main_app/utils.py`:
```python
def get_client_ip(request):
    """Получение IP адреса клиента с учетом прокси"""
    x_forwarded_for = request.META.get('HTTP_X_FORWARDED_FOR')
    if x_forwarded_for:
        ip = x_forwarded_for.split(',')[0].strip()
    else:
        ip = request.META.get('REMOTE_ADDR')
    return ip
```

### 3. Дублирование логики проверки Cloudflare Turnstile

**Проблема:** Почти идентичный код в двух формах:
- `my_site/accounts/forms.py` - `CustomUserCreationForm.clean_cf_turnstile_response()`
- `my_site/main_app/forms.py` - `OrderForm.clean_cf_turnstile_response()`

**Решение:** Создать базовый mixin или утилиту:
```python
# my_site/main_app/mixins.py
class TurnstileMixin:
    def clean_cf_turnstile_response(self):
        # Общая логика проверки
        ...
```

### 4. Использование print() вместо логирования

**Проблема:** В коде множество `print()` для отладки и логирования:
- `my_site/main_app/views.py` - множество print()
- `my_site/main_app/forms.py` - print() для Turnstile
- `my_site/accounts/views.py` - print() для отладки
- `my_site/main_app/middleware.py` - print() для ошибок

**Последствия:**
- В production print() может не работать или засорять логи
- Нет уровней логирования (DEBUG, INFO, ERROR)
- Сложно отслеживать проблемы

**Решение:** Использовать Django logging:
```python
import logging
logger = logging.getLogger(__name__)

# Вместо print()
logger.debug("Debug message")
logger.info("Info message")
logger.error("Error message", exc_info=True)
```

### 5. Отсутствие оптимизации запросов к БД

**Проблема:** В некоторых views нет `select_related()` или `prefetch_related()`:
- `free_soft()` - просто `FreeSoftware.objects.all()`
- `business_soft()` - просто `BusinessSoftware.objects.all()`
- `my_projects()` - просто `Project.objects.all()`

**Решение:** Если в шаблонах используются связанные объекты, добавить:
```python
projects = Project.objects.select_related('author').all()  # пример
```

---

## 📋 Рекомендации по улучшению

### 6. Обработка ошибок в middleware

**Файл:** `my_site/main_app/middleware.py` (строки 63-65)

**Проблема:** Ошибки логирования просто игнорируются через `print()`

**Решение:** Использовать proper logging и возможно добавить sentry/rollbar для мониторинга ошибок.

### 7. Безопасность: проверка Turnstile в production

**Проблема:** В формах есть проверка на наличие ключей Turnstile, но если ключи не настроены, форма все равно работает (только с предупреждением).

**Рекомендация:** В production обязательно настроить Turnstile ключи и возможно добавить проверку в settings:
```python
if not DEBUG and not CLOUDFLARE_TURNSTILE_SECRET_KEY:
    raise ImproperlyConfigured("Turnstile keys must be set in production")
```

### 8. Валидация данных в формах

**Хорошо:** Есть валидация через формы Django и Turnstile.

**Можно улучшить:** Добавить rate limiting на уровне middleware для защиты от спама (не только по IP, но и по другим параметрам).

### 9. Структура проекта

**Хорошо:**
- Четкое разделение на приложения (main_app, accounts, chat, contract_maker)
- Использование i18n для мультиязычности
- Настройки через переменные окружения

**Можно улучшить:**
- Вынести общие утилиты в отдельный модуль
- Создать базовые классы/mixins для повторяющейся логики

### 10. Тестирование

**Проблема:** Не видно тестов для критичных функций (формы, middleware, views).

**Рекомендация:** Добавить unit-тесты для:
- Проверки Turnstile
- Логики получения IP
- Middleware
- Форм

---

## ✅ Что сделано хорошо

1. ✅ Использование переменных окружения для секретов
2. ✅ Мультиязычность (i18n)
3. ✅ Защита от спама через Turnstile и лимиты по IP
4. ✅ Логирование действий пользователей (скачивания, переходы)
5. ✅ Использование Cloudinary для медиа-файлов
6. ✅ Правильная структура Django приложений
7. ✅ Использование WhiteNoise для статики
8. ✅ Настройка sitemap.xml и robots.txt

---

## 🎯 Приоритет исправлений

1. **СРОЧНО:** Исправить ошибку с полями в middleware.py (#1)
2. **ВАЖНО:** Заменить print() на logging (#4)
3. **ВАЖНО:** Вынести дублирующийся код в утилиты (#2, #3)
4. **ЖЕЛАТЕЛЬНО:** Добавить оптимизацию запросов (#5)
5. **ЖЕЛАТЕЛЬНО:** Улучшить обработку ошибок (#6)
6. **МОЖНО:** Добавить тесты (#10)

---

## 📝 Дополнительные замечания

- В `requirements.txt` есть дубликат `requests==2.32.3` (строки 15 и 26)
- Файл `.env` должен быть в `.gitignore` (уже есть ✅)
- Есть временные файлы типа `fucking_test.py` - можно удалить
- В git status виден файл `django.po.bak` - можно добавить `*.bak` в `.gitignore`

---

## 🔧 Быстрые исправления

### Исправление #1 (критическое):
```python
# my_site/main_app/middleware.py, строка 57-58
# БЫЛО:
view.last_viewed_at = now()
view.last_viewed_ip = ip

# ДОЛЖНО БЫТЬ:
view.last_viewed = now()
# Убрать строку с last_viewed_ip или добавить поле в модель через миграцию
```

### Исправление #2 (улучшение):
Создать `my_site/main_app/utils.py` и вынести туда общие функции.

---

**Составитель:** AI Code Reviewer  
**Статус:** Требуется исправление критических ошибок перед деплоем
