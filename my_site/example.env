# Пример переменных. Скопируйте в .env и подставьте свои значения.
# Имена переменных должны совпадать точно (без опечаток).
#
# Сайт (продакшен https://liuba.site):
# ALLOWED_HOSTS=liuba.site,www.liuba.site,127.0.0.1,localhost
# DJANGO_CSRF_TRUSTED_ORIGINS=https://liuba.site,https://www.liuba.site
#
# Реквизиты исполнителя (подставляются в договоры и акты):
CONTRACT_MAKER_FULL_NAME=Фамилия Имя Отчество
CONTRACT_MAKER_SHORT_NAME=Ф.И. О.
CONTRACT_MAKER_INN=123456789012
CONTRACT_MAKER_ADDRESS=г. Минск, ул. Примерная, д. 1
CONTRACT_MAKER_BASIS=законодательства о самозанятости
CONTRACT_MAKER_BANK_DATA=Банк ОАО "Пример"\nР/с BY00XXXX00000000000000000000
#
# Для переноса строк в BANK_DATA используйте \n (обратный слэш + n).
#
# Telegram: уведомления о регистрации (куда слать) и бот @myregibot (кому отвечать)
# TELEGRAM_BOT_TOKEN=...
# TELEGRAM_CHAT_ID=...   — чат для уведомлений (если группа — id группы)
# TELEGRAM_BOT_ALLOWED_CHAT_ID=...   — твой user id в личке с ботом (узнать: @userinfobot). Если не задан, бот использует TELEGRAM_CHAT_ID.
#
# Тест API для бота (добавление клиента). Если задан — бот должен передать в заголовке X-Bot-Token или в JSON "token"
# CONTRACT_MAKER_BOT_TOKEN=your_secret_token

# Альтернативные имена (без префикса) тоже работают:
# FULL_NAME=Фамилия Имя Отчество
# SHORT_NAME=Ф.И. О.
# INN=123456789012
# ADDRESS=г. Минск, ул. Примерная, д. 1
# BASIS=законодательства о самозанятости
# BANK_DATA=Банк ОАО "Пример"\nР/с BY00XXXX00000000000000000000
